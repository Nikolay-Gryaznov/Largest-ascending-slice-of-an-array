A slice is a sequence of consecutive array elements, where each next element is greater than the previous one.

Input data format:

A natural number N is given, followed by N integers.

Output data format:

On the first line print the length of the maximum slice

Next, output the content of the slice / slices, separating the elements with one space, each slice on a new line